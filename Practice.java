class Math
{
	public static void main(String arg[])
	{
		double x;
		double res1;
		double res2;
		x=12.3;
		res1=math.ceil(x);//rounds to next number
		res=math.floor(x);//rounds to same number
		System.out.printlin("ceiling; "+res1);
		System.out.printlin("floor; )
	}
}

